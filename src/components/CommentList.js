import React, { PropTypes } from 'react';
import './CommentList.css';

export const CommentList = React.createClass({
  render() {
    return (
      <div>
        CommentList
      </div>
    );
  }
});
