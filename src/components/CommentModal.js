import React, { PropTypes } from 'react';
import './CommentModal.css';

export const CommentModal = React.createClass({
  render() {
    return (
      <div>
        CommentModal
      </div>
    );
  }
});
